# Dedekind Studio Backend (Python)
# Python 3.10+ erforderlich

flask>=2.0
flask-cors>=3.0
torch>=2.0
matplotlib>=3.5
scipy>=1.9
sympy>=1.10

# Dedekind Jupyter Kernel (für Spyder / Jupyter / Dedekind Studio)
ipykernel>=6.0

# Optional: GNN-Beispiele (gnn_molecule_demo.ddk, gnn_materials_demo.ddk, graph_shape_test.ddk).
# Andere Dedekind-Features funktionieren auch ohne torch_geometric.
torch_geometric>=2.7

# Optional: MD-Bruecke (md_lennard_jones_demo.ddk, md_simulate_lj_test.ddk).
# Wird nur fuer md_simulate_lj() benoetigt.
openmm>=8.0

# Optional: Labeled Tensors (labeled_tensors_demo.ddk, labeled_tensors_test.ddk).
# Wird nur fuer labeled_tensor() / LabeledTensor[...]-Annotation benoetigt.
xarray>=2024.0
