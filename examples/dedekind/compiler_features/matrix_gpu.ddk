fn main() {
    //Defining matrices
    A = [[1.0, 2.0], [3.0, 4.0]]
    B = [[5.0, 6.0], [7.0, 8.0]]

    print("Calculate matrix multiplication on the GPU...")
    
    //Moving to GPU and Matmul
    result = matmul(A.gpu(), B.gpu())
    
    print("Result matrix:")
    print(result)
}
main()
